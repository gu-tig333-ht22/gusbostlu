import 'package:my_app/main.dart';

/*List<itemList>? _filterList(list, filterBy) {
    if (filterBy == "All") 
    return list;
    if (filterBy ==  "Done") 
    list.where((item)) => item.value == true).tolist();
    if (filterBy ==  "NotDone") 
    list.where((item)) => item.value == fals).tolist();
    return null;*/